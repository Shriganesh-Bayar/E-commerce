const pool = require('../config/db');
const { query } = require('query');

const Seller = {
    addNew: async () => {

    },

    updateProduct: async () => {

    },

    removeProduct: async () => {

    }
};

module.exports = Seller;