const pool = require('../config/db');
const { query } = require('query');
 
const Customer = {
    addCart: async () => {

    },

    buyCart: async () => {

    }, 

    removeCart: async () => {

    },

    changeNumberinCart: async () => {

    }
};

module.exports = Customer;